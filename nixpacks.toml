[variables]
NIXPACKS_PHP_ROOT_DIR = "/app/public"

[phases.build]
cmds = [
  "...",
  "php artisan storage:link || true",
  "php artisan optimize:clear",
]

[start]
cmd = "sh /app/scripts/coolify-start.sh"
